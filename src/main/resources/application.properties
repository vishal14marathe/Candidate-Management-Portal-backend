# Server Configuration
server.port=8080

# PostgreSQL Database Configuration - CORRECTED URL
spring.datasource.url=jdbc:postgresql://dpg-d48tqhggjchc73f3dts0-a.oregon-postgres.render.com:5432/springboot_db_dnio
spring.datasource.username=springboot_db_dnio_user
spring.datasource.password=BQQlLP6epH4lvDXr4rFeVvyRgyoPZcHC
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Render.com PostgreSQL Optimization
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads/resumes

# JWT Configuration
jwt.secret=3f5e8a7c-9b2d-4c3a-8a1e-2d9f6c7b8e9d
jwt.expiration=604800000

# Email Configuration (Update with your email credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Admin Email
admin.email=codersflex@gmail.com

# Logging
logging.level.com.project=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Spring Security (if needed)
spring.security.oauth2.client.registration.google.client-id=your-google-client-id
spring.security.oauth2.client.registration.google.client-secret=your-google-client-secret

# CORS Configuration (for frontend)
app.cors.allowed-origins=http://localhost:5173,http://localhost:8080

# Application-specific properties
app.name=Candidate Management System
app.version=1.0.0